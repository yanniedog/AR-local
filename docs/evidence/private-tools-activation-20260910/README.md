# September 10 private-tool activation

PR668 merged at `d930716ae1d32c19dd73b95ea5d719f6209eb179` with Linux and
Windows product CI passing. `prepare.py` materialized that exact receiver and
wrote the D-023 decision, immutable configuration, tool contract and deployment
before the task action was changed. The five substantive PR668 review threads
were individually disposed and resolved. PR669 fixes the auxiliary
dependency-only null-device transition; the installed current-mode guard does
not require that follow-up or another receiver transition.

`install.ps1` uses the reviewed ordinary-user action transaction. Installation
passed at 09:09:19 Hobart, preserving task principal, settings and triggers.
The hidden Windows PowerShell probe passed at 09:13:32. The original decision
and `manual-start-intent.json` identify the private tools and manual origin
before the 09:14:25 task start. Neither operation required elevation.

The original 06:00 failure remains in the preceding evidence directory. It is
not this manual execution. The September 9 operational backup and its D-022
controlled-acceptance limitation also remain historical evidence.

`capture-terminal.py` is a read-only, one-shot evidence collector. It refuses a
running task, requires a completed guard bound to the activated deployment,
checks that task and captured metadata are unchanged across its read, and
creates a new snapshot without acquiring any live receiver locks. Its output
does not itself prove archive restoration or successful receipt binding.

`live-dashboard.json` records the separate successful live Pi browser and smoke
checks. September 10 publication/download identities are in the preceding
`git-dependency-20260910/publication-check.json`. No connected phone state was
available, so app refresh success is not claimed. No Pi deployment, restart,
ingest replay or physical media action was performed by this closeout.

Validation before terminal capture: 46 focused dependency and preserved-handoff
tests passed. Exact PR669 Linux and Windows product CI passed on `a368bdd4e`;
later evidence commits must still satisfy their applicable checks.

The manual run ended at 09:29:06 with overall `FAIL`: all three component
archives passed offline restore, but the final control comparison was `STALE`.
The 4.90 GB observation restored 10,280 files and the selected September 10
revision. Control and macro also passed their individual checks. The frozen
terminal packet preserves the failed aggregate result and all component
receipts; successful aggregate receipt binding is not claimed.

The read-only comparison checked 1,676 control data files. Only
`cdr-recovery-status.json` and already-excluded `runtime_health.json` changed.
The former differed solely in `checked_at`; every recovery fact was identical.
The follow-up preserves that raw file and adds its normalized facts to control
metadata, excludes only its observation timestamp from the revision, and
checks the normalized identity again against the restored raw file. Historical
manifests retain their original revision calculation. The scheduled receiver
uses the producer's same revision function to avoid divergence.

The fix is source-only at this entry. It requires a new exact receiver decision
and action-only installation before use; the currently installed v5 source is
unchanged. The original Pi helper bytes used by retired D012 evidence are
preserved in a hash-checked fixture. Validation: 206 focused backup/tool tests
plus 74 handoff/receipt tests passed; two platform-specific tests skipped.
