# XLSX files are written directly as Office Open XML ZIP packages. Pi service
# provisioning installs the system-Python jsonschema package before activation;
# this lock remains the development/CI version contract.
tzdata>=2024.1
jsonschema>=4.23,<5
zstandard==0.25.0
# Optional: app payload asset encryption (AR_LOCAL_PAYLOAD_ENC=1) needs the
# 'cryptography' package. Raspberry Pi OS ships it system-wide
# (python3-cryptography); it is imported lazily, so plaintext builds don't need it.
