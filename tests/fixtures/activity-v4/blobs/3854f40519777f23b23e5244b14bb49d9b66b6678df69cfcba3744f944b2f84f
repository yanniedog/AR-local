/** Produced only after validating the enclosing portfolio's complete routing contract. */
export interface AccountAuthority {
  packageInstances: ReadonlySet<string>;
  tdExternalFees: boolean;
  projectionAssumptionId?: string;
  savingsAssessments?: ReadonlySet<string>;
  balanceUncertain?: boolean;
}
