"""Private, source-bound product document evidence; no automatic publication.

Acquisition proves that bytes were retained, not that terms were interpreted.
The existing cdr_product_facts and cdr_product_changes remain the structured
CDR fact taxonomy and change engine. This package adds their missing document
provenance layer without modifying retained observations.
"""

SCHEMA_VERSION = 1
