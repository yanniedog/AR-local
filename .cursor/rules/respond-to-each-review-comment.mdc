---
description: "Mandatory thread closure: enumerate every CI/review finding, judge merits, reply in-thread before merge"
alwaysApply: true
---

# Respond to each piece of feedback before merge

Full bot reply standard matches Australian Rates: **`WORKFLOW.md`** step 6 and **`git-pr-workflow-default.mdc`**.

## What counts as feedback

- Required workflow failures (tests, lint, CI).
- Inline comments and reviews from humans.
- Bot comments: Gemini Code Assist, Codex connector, Copilot, CodeRabbit, Sourcery, Greptile, security scanners.

## Mandatory per finding

1. Enumerate distinct threads (merge verbatim duplicates into one reply).
2. Judge: implement / defer / decline (with reason — not silence).
3. Reply in-thread with the disposition, then **Resolve** the thread on GitHub. `required_conversation_resolution` is enabled, so resolution is what unblocks merge — a reply alone does not. If inline replies are unavailable: `## Feedback responses` section in the PR body, and still resolve.

## Do not

- Claim "no actionable feedback" from a top-level summary without checking all inline file-level threads.
- Dismiss any bot as "non-blocking noise" without per-item resolution.
- Merge with identified threads unaddressed and un-declined.
- Push empty "re-trigger" commits: the feedback gate self-heals on review/comment events, and `cancel-in-progress: false` means a `BLOCKED`-but-green PR with 0 unresolved threads just needs the next event or `gh run rerun <run-id>` on the PR head (`gh pr merge --auto` only enables auto-merge; it does not re-run a stale gate). After a resolve-only round, re-run via an inline review-thread reply (`pull_request_review_comment`, runs on the PR head), a push, or `gh run rerun <run-id>` of the existing PR-head run (GitHub emits no webhook on resolve). A top-level PR comment (`issue_comment`) and a bare `gh workflow run … -f pr_number` run against the default branch, so their check won't attach to the PR head.
