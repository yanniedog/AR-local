---
description: "Use the Pi status API as the runtime acceptance target"
alwaysApply: true
---

# Pi acceptance target

- Primary: `http://100.78.28.10/` through nginx.
- Direct backend: loopback-only `http://127.0.0.1:8808/` on the Pi.
- Use local loopback only for explicitly requested local development.
- Verify `/healthz` and `/api/status` with `npm run verify:pi`.
- Never treat the public Australian Rates site as AR-local acceptance.
