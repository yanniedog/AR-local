---
description: "Hard guard: do not stop after PR/CI — run ship:closeout:strict; exit 2 means continue the full ship bar"
alwaysApply: true
---

# No early stop after PR or CI green

Before ending any turn on a shipping task:

```sh
npm run ship:closeout:strict
npm run pr:gates:check -- --pr <n>
```

from repo root.

- **Exit 0** — clear to stop (for these guards).
- **`ship:closeout:strict` exit 2** — open PR exists; continue **`WORKFLOW.md`** steps 5–9.
- **`pr:gates:check` exit 2** — only GitHub-owned CI is still settling; park and re-run the single-shot audit later.
- **`pr:gates:check` exit 3** — actionable CI, conflict, or feedback-thread work remains; fix it before stopping.

**Never** end with "done", "shipped", "PR opened", "CI green so we're good", or "handing off the PR" while an open PR exists and you have merge ability.

Full ship bar: **`WORKFLOW.md`**.
