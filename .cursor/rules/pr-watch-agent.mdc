---
description: PR watch agent — continuous open-PR bot closure, merge, Pi deploy (invoke or chief delegation).
globs:
alwaysApply: false
---

# PR watch agent

When the user says **"run pr watch"**, **"watch open PRs"**, or when **chief/workflow-orchestrator** delegates continuous PR babysit + merge + Pi sync:

1. Read [`.cursor/skills/pr-watch-agent/SKILL.md`](../skills/pr-watch-agent/SKILL.md).
2. Run one bounded `npm run pr:watch-once`; do not start polling loops.
3. For each open PR (oldest first): pr-fix patterns → `wait-for-bots` → `pr:bot-feedback-check` → `pr:gates:check` → `pr:merge` (squash auto-merge) only at exit **0**.
4. After merges: run drift verification only. Production activation requires the separate exact-commit canary workflow; never deploy merely because a PR merged.

**Not a replacement for chief** on path locks or concurrent `agent/*` branch writers. One pr-watch worker at a time per repo.

**Hooks:** `.cursor/hooks.json` remains empty unless `AR_LOCAL_COORDINATION_HOOKS=1`; chief may spawn this agent in the background when `gh pr list --state open` is non-empty.
