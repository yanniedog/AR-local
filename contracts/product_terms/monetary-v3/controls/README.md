# Monetary wire3 — REVIEW DRAFT ONLY

No schema freeze, producer/app code change, source acquisition, approval, runtime execution or publication. The JSON files define initial savings policy shape and reserved capability routing. `positive-technical-example.json` contains placeholder identities; it is structurally valid engineering data, NOT an admissible source graph or approved bank subject. `draft-validation-receipt.json` records only schema checks.

## Files

- common: frozen-concept copies (not modified v2 files), scope/current routing, evidence/rule types.
- authority-graph: retained observation OR dated official clause, per-field/dated-posting coverage, explicit supersession and independently sourced completed-period bound.
- savings-policy: base-only AUD historical holding, exact source policy, three engine-supported rounding modes, discard residue only, no fee/bonus/intro/offset/linked account.
- subject/asset/index/shard: closed savings capability, exact subject/approval association; no arbitrary LedgerContract.
- namespace: one wire3 container with separately routed savings/mortgage/portfolio descriptor inventories. Future route policy bodies remain unsupported; current savings consumer never fetches them. Frozen v2 remains separate.

## Required semantic gates before admission or execution

JSON Schema cannot enforce these relationships. They are REQUIRED, not optional documentation or assumed approvals:

1. Canonical UTF8 sorted-key SHA256 IDs: subject excludes id; authority excludes id; graph/asset exclude identitySha256. Scope ID hashes `["monetary-scope-v3", capability, scope]`. Exact recomputation, unique IDs, no duplicate scope subjects. approval.subjectId and authorityGraphSha256 equal contained canonical identities. Outer product/routing/index/shard/capability map equality; sorted exact evidence document/revision inventory. Rule IDs unique/letter-safe,512nodes/depth16; typed definitions/units/roles/branch references checked.
2. Current routing matches original verified adopted core/details/product record and source generation/export digest. Historical authority members are INDEPENDENT of that current snapshot. Decode/hash/length each retained member before use; every historical observation/raw source/core/details/row and official document/clause must match its member bytes and product/cohort/tier/package scope. No orphan/unused or missing required member. No customer data in public source members.
3. Source-owned scope dates and completedPeriod, valid civil dates1900..2200/strict timestamps. asOf is reviewed data-completeness provenance, NOT a bank effective date or user clock. Calculation end cannot exceed source-owned completedThroughExclusive. Capture-only observations do not fill intervening days. Every rate and policy field has full explicit interval coverage; each posting event additionally has exact coverage for posting/calendar/residue/balance rules. Authority scope covers its interval; each policy interval fits its authority and overall scope; contiguous/no overlap/no gaps. Contradictory overlap refuses unless exact source-backed supersession covers it. No newest-fetch precedence.
4. Initial adapter must require equal InterestPolicy settings across all intervals except postingDates/evidence, because the current engine has one global InterestPolicy. Likewise dailyAccrualRounding is global to SavingsRateSchedule. Combine proven posting dates only; a changed rounding/daycount/basis policy refuses rather than being ignored. Rates and tier allocation may change at explicit reviewed interval boundaries. Tiers strictly ascending upper-inclusive cents from zero with final null UNLIMITED as a source-declared boundary, not unknown. Decimal equality to original row or explicit dated clause values; no freeform tier parsing.
5. Field evidence includes explicit dailyAccrualScale/accrualRounding coverage under dailyAccrualRounding, not just aggregation. All absent-operation policies (fees, deferred fees, intro, bonus, linked accounts) must be proven for the entire request and posting horizon. Fee inventory must be complete independently reviewed source coverage; writing `all_fees` cannot self-authorize absence. Remainder policy only discard-with-roundingAdjustment; carry/unknown refuses. Null daily-rate or daily-accrual rounding means explicit source-reviewed no-stage rounding, NOT unavailable data.
6. Actual local opening amount/dates and confirmed account schedule derive scenario-owned facts, never saved profile roles. Explicit zero/false preserved. User confirmations do not establish public source dates or continuity. Nonzero opening accrual, movements, withholding, rate mismatch, unknown confirmation, account closure, future forecast and material source gap refuse complete execution. Before-tax holding receipt only, no payout/offer/net-cost ranking claim.
7. Closed capability/adapter/version allowlist, independent review CAS/current revocation, actual typed adapter positive/holdout/refusal benchmark and retained code closure. Adding wire3 requires additive storage migration002 and transactional unified-view rebuild preserving existing v1/v2 rows; never alter frozen001 CHECK constraints/DDL. Current producer v2 source_snapshot verifier stays unchanged; version3 gets a distinct bounded authority-graph verifier through the SAME registry/controller.

## Whole-operation limits (enforce before allocation/download, not per subject only)

For one admission/review/calculation operation, union/deduplicate referenced IDs and enforce ALL limits across every requested subject:64 authorities,512 retained members,366 observations,256 documents,128 rate/policy intervals,366 posting events,64 supersessions,512rule nodes/depth16. Raw retained-member bytes<=32MiB and decoded bytes<=24MiB total,2MiB per member. Reject before decoding when declared sums exceed limits; verify actual sizes/hash while bounded streaming, abort oversize before unbounded allocation. Shared members count once by identical verified identity, never once per falsely different alias. Duplicate contradictory descriptors refuse.

Public transport:64KiB entire manifest;512KiB compressed+512KiB inflated per index/shard;8MiB compressed WHOLE manifest asset inventory including legacy/v2/allv3routes;24MiB entire expanded snapshot. Subject<=256KiB and product asset/shard<=512KiB encoded UTF8; max32 subjects/product. Unsupported capability descriptors still count toward manifest bounds but are not downloaded or interpreted. Exact route/kind/date/hash-prefix filename, immutable revision1..999999, recomputed bundle and source generation checks; global unique names and no unused shard inventory. Any new monetary namespace failure leaves core adoption and frozen eligibilityv2 independent.

Use the existing engine's incremental receipt limits too; do not claim source graph limits alone cap ledger allocation. Initial horizon<=366days and no supplied movements narrows work. No partial/truncated result marked complete when any limit is exceeded.

## Review status and unresolved source evidence

Draft-only shape validation:8 schemas valid; one positive technical subject valid;8 malformed-policy/descriptor cases rejected. The additional negative inventory lists semantic/adapter/source gates not implemented here. No actual savings adapter or review exists.

Retain `../SAVINGS-CURRENT-POLICY-DISCOVERY-20260915.md`: ordinary Macquarie365/marginal/closing-before-posting differs from Cash Hub. Rounding tie/stage, residue and successor brackets remain unresolved. No placeholder in this draft establishes those policies. Producer inspection/freeze follows its current PR readiness and root review.


## Revision2 review corrections

The exact prior draft and receipt are retained in `revisions/r1-before-opening-posting-fix/`. No original freeze exists or is implied.

- Opening settlement state: whenever any covered interval uses cleared_only, the local input must explicitly confirm all opening funds are cleared at the start. False, absent or unknown confirmation refuses the initial no-movement model. No movement during the interval does not rule out previously pending funds settling later. The adapter may omit this question only if every covered interval explicitly includes cleared_and_uncleared balances; it must not synthesize confirmation from product terms or profile answers. Example now exercises cleared_only with explicit true. This is a private input requirement, not a bank-source claim.
- Complete posting inventory: policy.postingInventory covers the entire source-approved holding scope. Its dueDates must equal ALL dates derived from its source rule, using from-inclusive/to-exclusive boundaries and the exact no-business-adjustment policy. calendar_month_end derives every calendar month-end in coverage; explicit_dated_source_schedule requires independently proven completeness over sourceFrom/sourceToExclusive and exact filtering. The adapter filters this proven complete inventory to the requested horizon; union of interval InterestPolicy.postingDates must equal it without duplicates. Merely having evidence for each listed event is insufficient. Empty dueDates is valid only when the full source rule proves no date is due; omitted dates cannot silently remove compounding. Each due event still needs per-field policy coverage. A due date equal to the calculation start is included; equal to endExclusive is excluded, with zero opening unposted accrual confirmed separately.
- Namespace index/shard filename patterns and shard keys now specialize to the enclosing capability AND role. Exact descriptor hash/date/prefix association remains a required semantic check.
