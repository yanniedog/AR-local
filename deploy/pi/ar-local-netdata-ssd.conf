# AR-local portable SSD paths for Netdata metrics DB (install-pi-netdata.sh).
# Paths are substituted at install time; do not edit on the Pi by hand.

[directories]
    cache = @NETDATA_CACHE@
    lib = @NETDATA_LIB@
    log = @NETDATA_LOG@
