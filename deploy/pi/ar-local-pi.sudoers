# Passwordless sudo for Pi user pi (agents / npm run pi:deploy).
# Install: deploy/pi/install-pi-sudoers.sh  (one interactive sudo on first run)
# Validates with: sudo visudo -cf /etc/sudoers.d/ar-local-pi

pi ALL=(ALL) NOPASSWD: ALL
