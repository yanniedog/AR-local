# WebSocket upgrade map for Netdata /netdata/ proxy (included from http {} via conf.d).
# Installed by deploy/pi/install-pi-dashboard-proxy.sh
map $http_upgrade $http_connection_upgrade {
    default upgrade;
    ''      close;
}
